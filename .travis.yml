language: php

php:
  - 7.0
  - 7.1

env:
  global:
    - TARGET=test

matrix:
  fast_finish: true

sudo: false

cache:
  directories:
    - $HOME/.composer/cache

install: composer update --prefer-dist --no-interaction $COMPOSER_FLAGS